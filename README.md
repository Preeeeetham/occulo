
  # High-Fidelity UI/UX Design

  This is a code bundle for High-Fidelity UI/UX Design. The original project is available at https://www.figma.com/design/UiJAlXiN5QzEg8tBssYOkA/High-Fidelity-UI-UX-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  